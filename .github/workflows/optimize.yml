name: Optimierer (nächtlich)

# Walk-Forward-Selbstoptimierung nach US-Börsenschluss: Universum nach
# Liquidität wählen, Bars nachladen, Champion bestimmen, Champion + Bericht
# nach Firestore veröffentlichen. Läuft komplett im GitHub-Actions-Freikontingent —
# kein Server. Die Engine-Function liest den Champion aus `meta/champion`.
#
# 23:00 UTC deckt Sommer- (Schluss 20:00 UTC) und Winterzeit (21:00 UTC) ab.

on:
  schedule:
    - cron: '0 23 * * 1-5'
  workflow_dispatch:
    inputs:
      samples:
        description: 'Stichproben je Fold (leer = Config)'
        required: false
        default: ''

concurrency:
  group: optimize
  cancel-in-progress: false

jobs:
  optimize:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      ALPACA_API_KEY: ${{ secrets.ALPACA_API_KEY }}
      ALPACA_SECRET_KEY: ${{ secrets.ALPACA_SECRET_KEY }}
      FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - name: Abhängigkeiten installieren
        run: npm ci

      # `var/universe.json` liegt mit im Cache: Es ist der Bestand für die
      # Hysterese der nächsten Wahl. Fehlt es (erster Lauf, geänderte Config),
      # gilt das Universum aus der Config als Bestand — kein Ausfall.
      - name: Bars-Cache wiederherstellen
        uses: actions/cache@v4
        with:
          path: |
            var/bars
            var/universe.json
          key: bars-${{ hashFiles('config/platform.yaml') }}-${{ github.run_id }}
          restore-keys: |
            bars-${{ hashFiles('config/platform.yaml') }}-

      # Die Alpaca-Keys sind hier REPOSITORY-Secrets — das Altsystem brauchte sie
      # nur als Functions-Secrets (universumSync), also sind sie neu anzulegen.
      # Nächtlich fehlend ⇒ Warnung und Abbruch ohne Rot (sonst färbt sich das
      # Repo jede Nacht rot, bis sie stehen); von Hand gestartet ⇒ harter Fehler,
      # weil dann jemand ein Ergebnis erwartet.
      - name: Keys prüfen
        id: keys
        run: |
          if [ -z "$ALPACA_API_KEY" ] || [ -z "$ALPACA_SECRET_KEY" ]; then
            if [ "${{ github.event_name }}" = "schedule" ]; then
              echo "::warning::ALPACA_API_KEY / ALPACA_SECRET_KEY fehlen als Repository-Secrets — Lauf übersprungen. Ohne Champion handelt niemand."
              echo "skip=1" >> "$GITHUB_OUTPUT"
              exit 0
            fi
            echo "::error::ALPACA_API_KEY / ALPACA_SECRET_KEY fehlen als Repository-Secrets."
            exit 1
          fi
          printf 'ALPACA_API_KEY=%s\nALPACA_SECRET_KEY=%s\nALPACA_ALLOW_LIVE=0\n' "$ALPACA_API_KEY" "$ALPACA_SECRET_KEY" > "$RUNNER_TEMP/.env"
          chmod 600 "$RUNNER_TEMP/.env"

      # Zuerst das Universum: Tagesbars für den ganzen Kandidatenpool, Rang nach
      # Median-Dollarumsatz, die liquidesten `maxSymbols` gewinnen. NIE nach
      # Ertrag — siehe src/universe/select.ts. Alles danach rechnet mit dieser
      # Auswahl (`--universe`), auch der Firestore-Sync.
      - name: Handelsuniversum wählen
        if: steps.keys.outputs.skip != '1'
        run: node src/cli.ts universe --config config/platform.yaml --env "$RUNNER_TEMP/.env" --home var

      - name: Bars und Kalender nachladen
        if: steps.keys.outputs.skip != '1'
        run: node src/cli.ts fetch --config config/platform.yaml --env "$RUNNER_TEMP/.env" --home var --universe var/universe.json

      - name: Walk-Forward-Optimierung
        if: steps.keys.outputs.skip != '1'
        env:
          # Nie direkt in den Shell-Text interpolieren (Secreview 2, G5): Der Wert kommt als Umgebungsvariable
          # und muss eine ganze Zahl sein, sonst bricht der Schritt ab.
          SAMPLES_OVERRIDE: ${{ github.event.inputs.samples }}
        run: |
          if [ -n "$SAMPLES_OVERRIDE" ]; then
            case "$SAMPLES_OVERRIDE" in
              ''|*[!0-9]*) echo "::error::samples muss eine ganze Zahl sein: '$SAMPLES_OVERRIDE'"; exit 1 ;;
            esac
            echo "Hinweis: samples-Override ($SAMPLES_OVERRIDE) aus workflow_dispatch wird über die Config gesetzt."
            sed -i "s/^  samples: .*/  samples: $SAMPLES_OVERRIDE/" config/platform.yaml
          fi
          node src/cli.ts optimize --config config/platform.yaml --env "$RUNNER_TEMP/.env" --home var --universe var/universe.json --equity 25000

      - name: Bericht als Artefakt
        # `always()`: Gerade der Lauf, der NICHTS gemessen hat (Rückgabecode 3),
        # ist der, dessen Bericht man lesen muss. Die Veröffentlichungs-Schritte
        # darunter tragen dieses `always()` bewusst NICHT — aus einem Lauf ohne
        # Messung darf nichts nach Firestore.
        if: always() && steps.keys.outputs.skip != '1'
        uses: actions/upload-artifact@v4
        with:
          name: optimize-report-${{ github.run_id }}
          if-no-files-found: warn
          path: |
            var/champion.json
            var/universe.json
            var/reports/optimize-*.md
            var/reports/universe-*.md
          retention-days: 90

      - name: Service-Account bereitstellen
        if: steps.keys.outputs.skip != '1' && env.FIREBASE_SERVICE_ACCOUNT != ''
        run: printf '%s' "$FIREBASE_SERVICE_ACCOUNT" > "$RUNNER_TEMP/firebase-sa.json"

      # Reihenfolge ist wichtig: erst das Urteil (WIE gehandelt wird), dann das
      # Universum (WAS gehandelt wird). Scheitert der zweite Schritt, steht ein
      # Champion bereit, der den alten Korb abdeckt — Symbole ohne Urteil werden
      # wegen `allowWithoutChampion: false` einfach nicht gehandelt. Andersherum
      # hätten die abgegangenen Symbole ihr Urteil verloren, während sie noch im
      # gehandelten Universum stehen.
      - name: Champion und Bericht nach Firestore
        if: steps.keys.outputs.skip != '1' && env.FIREBASE_SERVICE_ACCOUNT != ''
        env:
          GOOGLE_APPLICATION_CREDENTIALS: ${{ runner.temp }}/firebase-sa.json
        run: node scripts/publish-champion.mjs --home var

      - name: Plattform-Config nach Firestore
        if: steps.keys.outputs.skip != '1' && env.FIREBASE_SERVICE_ACCOUNT != ''
        env:
          GOOGLE_APPLICATION_CREDENTIALS: ${{ runner.temp }}/firebase-sa.json
        run: node scripts/sync-engine-config.mjs --config config/platform.yaml --universe var/universe.json

      - name: Ohne Service-Account
        if: steps.keys.outputs.skip != '1' && env.FIREBASE_SERVICE_ACCOUNT == ''
        run: echo "::warning::FIREBASE_SERVICE_ACCOUNT fehlt — Champion nur als Artefakt, nicht veröffentlicht."
